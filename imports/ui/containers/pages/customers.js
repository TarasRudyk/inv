import { Meteor } from 'meteor/meteor';
import { createContainer } from 'meteor/react-meteor-data';

import Customers from '/imports/ui/pages/customers';

export default createContainer(() => {

  return {

  };
}, Customers);
